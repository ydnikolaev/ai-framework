# 🚀 Setup Guide — От нуля до деплоя

> Полное руководство по созданию нового проекта с ai-framework.

---

## 📋 Оглавление

1. [Подготовка (опционально)](#1-подготовка-опционально) — gum
2. [Создание проекта](#2-создание-проекта) — repo, setup.sh, SSH, первый коммит
3. [Настройка окружения](#3-настройка-окружения) — .env, боты, DNS
4. [Настройка сервера](#4-настройка-сервера) — VPS, Traefik
5. [Первый деплой](#5-первый-деплой) — GitHub Actions, webhook
6. [Чеклист](#6-чеклист)

---

## 1. Подготовка (опционально)

### 1.1 gum (интерактивный setup)

```bash
brew install gum
```

### 1.2 GitHub CLI (автоматизация)

```bash
brew install gh
gh auth login   # один раз на машине
```

> После авторизации `make setup-ssh` и `make setup-repo` работают автоматически.

---

## 2. Создание проекта

### Быстрый способ (одна команда)

```bash
# Если ai-framework ещё нет — склонируй один раз:
git clone git@github.com:ydnikolaev/ai-framework.git ~/ai-framework

# Теперь создавай проекты:
~/ai-framework/new-project.sh mybot
```

Скрипт автоматически:
- Создаёт папку и `git init`
- Подключает ai-framework как submodule
- Запускает `./setup.sh`
- Настраивает SSH ключи (`make setup-ssh`)
- Создаёт репозиторий на GitHub (`make setup-repo`)

> Требует: `gh auth login` (см. раздел 1.2)

---

### Ручной способ (пошагово)

<details>
<summary>Развернуть инструкцию</summary>

#### 2.1 Создать папку

```bash
mkdir mybot && cd mybot
git init
```

#### 2.2 Подключить ai-framework

```bash
git submodule add git@github.com:ydnikolaev/ai-framework.git ai-framework
cd ai-framework && ./setup.sh && cd ..
```

#### 2.3 Настроить SSH

```bash
make setup-ssh
```

#### 2.4 Создать репозиторий и push

```bash
make setup-repo
```

</details>

---

## 3. Настройка окружения

### 3.1 Заполнить .env

После `./setup.sh` создан `.env.example`. Скопируй и заполни:

```bash
cp .env.example .env
```

**Обязательные переменные:**

| Переменная | Описание | Пример |
|------------|----------|--------|
| `PROJECT_NAME` | Имя проекта (для Docker) | `mybot` |
| `DOMAIN` | Основной домен | `example.com` |
| `TELEGRAM_BOT_TOKEN` | Токен прод бота | `123:ABC...` |
| `TELEGRAM_BOT_TOKEN_DEV` | Токен dev бота | `456:DEF...` |
| `PROD_SERVER` | SSH до сервера | `deploy@1.2.3.4` |
| `PROD_DIR` | Папка на сервере | `mybot` |

### 3.2 Telegram боты

Создай **2 бота** в [@BotFather](https://t.me/BotFather):

| Бот | Username | Токен → |
|-----|----------|---------|
| Production | `@mybotBot` | `TELEGRAM_BOT_TOKEN` |
| Development | `@mybot_devBot` | `TELEGRAM_BOT_TOKEN_DEV` |

### 3.3 DNS записи

Добавь A-записи в DNS:

| Домен | Тип | Значение |
|-------|-----|----------|
| `example.com` | A | IP сервера |
| `dev.example.com` | A | IP сервера |
| `api-dev.example.com` | A | IP сервера |

---

## 4. Настройка сервера

> Выбери свой сценарий:
> - **Option A** — Новый сервер (инфраструктуры ещё нет)
> - **Option B** — Существующий сервер (Traefik уже развёрнут)

---

### Option A: Новый сервер (с нуля)

#### A.1 Первичная настройка VPS

```bash
# SSH на сервер
ssh root@YOUR_SERVER_IP

# Создать deploy юзера
adduser deploy
usermod -aG sudo deploy
usermod -aG docker deploy

# SSH ключи для deploy
mkdir -p /home/deploy/.ssh
cat >> /home/deploy/.ssh/authorized_keys << 'EOF'
ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ
EOF
chown -R deploy:deploy /home/deploy/.ssh
chmod 700 /home/deploy/.ssh
chmod 600 /home/deploy/.ssh/authorized_keys

# Установить Docker
curl -fsSL https://get.docker.com | sh

# Firewall
ufw allow 22
ufw allow 80
ufw allow 443
ufw enable
```

#### A.2 Shared Traefik (однократно на сервер)

```bash
# На сервере как deploy
ssh deploy@YOUR_SERVER_IP
mkdir -p ~/traefik && cd ~/traefik

# Скопируй файлы из infrastructure/traefik/ или запусти скрипт:
# (на сервере должен быть доступ к репозиторию)
./scripts/migrate-to-shared-traefik.sh
```

> См. [infrastructure/traefik/README.md](../infrastructure/traefik/README.md)

После этого переходи к **Шагу 5: Первый деплой**.

---

### Option B: Существующий сервер (Traefik уже есть)

Если сервер уже настроен и Traefik работает:

#### B.1 Добавить dev-роуты в Traefik

```bash
# Из папки проекта:
make add-dev-routes
```

Скрипт автоматически:
- Подключится к серверу
- Добавит роуты для `dev.${DOMAIN}` и `api-dev.${DOMAIN}`
- Перезапустит Traefik

Переходи к **Шагу 5: Первый деплой**.

---

## 5. Первый деплой

### 5.1 GitHub Actions

Добавь secrets в репозиторий:
- Settings → Secrets → Actions → **New repository secret**

| Secret | Значение |
|--------|----------|
| `DEPLOY_HOST` | IP сервера |
| `DEPLOY_USER` | `deploy` |
| `DEPLOY_KEY` | Приватный SSH ключ |

### 5.2 Деплой

```bash
git push origin main
# GitHub Actions запустит деплой автоматически
```

### 5.3 Webhook

```bash
make set-webhook-prod
```

---

## 6. Чеклист

### Перед началом
- [ ] SSH ключи настроены
- [ ] gum установлен (`brew install gum`)
- [ ] GitHub доступен по SSH

### Создание проекта
- [ ] Репозиторий создан на GitHub
- [ ] ai-framework подключен как submodule
- [ ] `./setup.sh` выполнен
- [ ] `.env` заполнен

### Telegram
- [ ] Прод бот создан, токен в `.env`
- [ ] Dev бот создан, токен в `.env`

### DNS
- [ ] `example.com` → IP сервера
- [ ] `dev.example.com` → IP сервера
- [ ] `api-dev.example.com` → IP сервера

### Сервер
- [ ] Docker установлен
- [ ] Shared Traefik запущен
- [ ] Firewall настроен
- [ ] Deploy юзер создан

### GitHub Actions
- [ ] `DEPLOY_HOST` secret добавлен
- [ ] `DEPLOY_USER` secret добавлен
- [ ] `DEPLOY_KEY` secret добавлен

### Финал
- [ ] `git push origin main` — деплой работает
- [ ] `make set-webhook-prod` — webhook установлен
- [ ] Бот отвечает!

---

## 🔗 Связанные документы

- [../infrastructure/](../infrastructure/) — Server infrastructure
- [../templates/.env.example](../templates/.env.example) — Все переменные
- [../core/operations/deployment.md](../core/operations/deployment.md) — Детали деплоя
