# 📖 Documentation Maintenance — AI Instructions

> **Роль:** Ты — Technical Writer и Knowledge Manager.
> **Твоя задача:** Поддерживать документацию актуальной, чтобы следующие сессии не наступали на те же грабли.

---

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО

> **После КАЖДОГО значимого изменения — ОБНОВИ ДОКУМЕНТАЦИЮ.**
> 
> Это НЕ опционально. Это часть завершения задачи.

---

## 📋 Когда обновлять документацию

### 🔴 ОБЯЗАТЕЛЬНО обновить

| Событие | Что обновить |
|---------|--------------|
| **Исправил баг** | `core/reference/troubleshooting.md` — добавь проблему и решение |
| **Нашёл workaround** | `docs/[framework]/README.md` — задокументируй quirk |
| **Изменил архитектуру** | `project/DECISIONS.md` — добавь ADR |
| **Добавил фичу** | `project/CHANGELOG.md` + `project/BACKLOG.md` |
| **🔴 Новая система/процесс** | `core/operations/[название].md` — **СОЗДАЙ ОТДЕЛЬНЫЙ ДОКУМЕНТ** |
| **Обновил версию пакета** | `docs/[framework]/README.md` — обнови версию и API |
| **Написал новый промпт** | `project/PROMPTS.md` — добаь промпт |
| **Нашёл паттерн/антипаттерн** | `core/stack/[tech].md` — добавь в DO's/DON'Ts |
| **Настроил окружение** | `core/operations/` — обнови гайд |

### 🟡 Рекомендуется обновить

| Событие | Что обновить |
|---------|--------------|
| Понял неочевидную вещь | `core/reference/troubleshooting.md` |
| Потратил время на дебаг | Добавь в FAQ чтобы не тратить снова |
| Написал полезный код | Добавь пример в соответствующий `docs/` |

---

## 📁 Куда писать

### Структура по типу знания

```text
ai-framework/
├── core/                    # Универсальные правила (НЕ зависят от проекта)
│   ├── stack/[tech].md     # Правила по технологии (DO's, DON'Ts)
│   ├── quality/            # Аудиты (performance, security, seo)
│   └── reference/          # Troubleshooting, cheat-sheet
│
├── docs/                    # API документация (версионируется)
│   └── [framework]/        # Конкретный фреймворк
│
└── project/                 # Специфика ЭТОГО проекта
    ├── DECISIONS.md        # Почему так сделали
    ├── PROMPTS.md          # Кастомные промпты
    └── CHANGELOG.md        # История изменений
```

### Правило выбора файла

```
Это специфично для проекта?
├── ДА  → project/
│   ├── Архитектурное решение? → DECISIONS.md
│   ├── Промпт для AI? → PROMPTS.md
│   └── Изменение кода? → CHANGELOG.md
│
└── НЕТ → core/ или docs/
    ├── Правило/паттерн? → core/stack/[tech].md
    ├── Проблема/решение? → core/reference/troubleshooting.md
    └── API справка? → docs/[framework]/README.md
```

---

## ✍️ Как писать

### Формат troubleshooting записи

```markdown
### [Короткое описание проблемы]

**Симптом:** Что видел пользователь/разработчик  
**Причина:** Почему это произошло  
**Решение:** Как исправить  

```code
// Пример решения
```
```

### Формат ADR (Architecture Decision Record)

```markdown
## ADR-XXX: [Название решения]

**Статус:** Accepted  
**Дата:** YYYY-MM-DD  

### Контекст
Какая проблема привела к решению?

### Решение
Что решили делать?

### Последствия
- ✅ Плюсы
- ⚠️ Минусы/ограничения
```

### Формат changelog записи

```markdown
## [X.Y.Z] - YYYY-MM-DD

### Added
- Что добавили

### Fixed
- Что исправили

### Changed
- Что изменили
```

---

## 🔄 Workflow обновления

### После исправления бага

```
1. Исправил баг
2. СТОП! Не переходи к следующей задаче
3. Спроси себя:
   - Это может повториться?
   - Это неочевидно?
   - Я потратил > 10 минут на дебаг?
4. Если ДА на любой → обнови документацию:
   - core/reference/troubleshooting.md (если универсально)
   - project/DECISIONS.md (если специфично для проекта)
5. Обнови project/CHANGELOG.md
```

### После реализации фичи

```
1. Реализовал фичу
2. СТОП! Проверь:
   - Использовал новый паттерн? → core/stack/[tech].md
   - Принял архитектурное решение? → project/DECISIONS.md
   - Написал кастомный промпт? → project/PROMPTS.md
3. Обнови:
   - project/CHANGELOG.md (Added: ...)
   - project/BACKLOG.md (отметь как выполнено)
```

### После обновления зависимости

```
1. Обновил пакет/версию
2. Проверь breaking changes
3. Обнови:
   - docs/[framework]/README.md (версия, API изменения)
   - project/CONFIG.yaml (версия)
   - project/CHANGELOG.md
```

---

## 📝 Именование файлов

### Core docs
```
core/stack/[technology].md     # Пример: golang.md, nuxt-vue.md
core/quality/[audit-type].md   # Пример: performance.md, security.md
core/reference/[topic].md      # Пример: troubleshooting.md, cheat-sheet.md
```

### Framework docs
```
docs/[framework-name]/         # kebab-case, как в npm
docs/[framework-name]/README.md
docs/[framework-name]/api.md   # Опционально
docs/[framework-name]/examples.md # Опционально
```

### Project docs
```
project/CONTEXT.md             # UPPERCASE
project/BACKLOG.md
project/CONFIG.yaml
```

---

## ⚡ Быстрые шаблоны

### Добавить в troubleshooting

```markdown
### Vue lifecycle warning в composables

**Симптом:** Warning "onMounted is called when there is no active component"  
**Причина:** Composable вызывается вне setup() контекста  
**Решение:** Вызывай composables только на верхнем уровне setup()

```typescript
// ❌ Bad
const handleClick = async () => {
  const { data } = useTelegram() // Ошибка!
}

// ✅ Good
const { data } = useTelegram() // На верхнем уровне
const handleClick = () => { ... }
```
```

### Добавить DO/DON'T в stack rules

```markdown
### DO's ✅
- **Новое правило:** Объяснение почему

### DON'Ts ❌
- **НЕТ [антипаттерн]:** Почему это плохо
```

---

## 🎯 Чеклист перед завершением задачи

- [ ] Код работает
- [ ] Тесты проходят
- [ ] **Документация обновлена:**
  - [ ] Troubleshooting (если был баг)
  - [ ] DECISIONS.md (если было решение)
  - [ ] CHANGELOG.md (если было изменение)
  - [ ] docs/[framework]/ (если узнал новое об API)
  - [ ] PROMPTS.md (если написал промпт)

---

## 🚫 Антипаттерны

### ❌ НЕ ДЕЛАЙ ТАК

1. **Не откладывай обновление документации**
   > "Сейчас закончу код, потом обновлю доки"
   > → Ты забудешь. Обновляй СРАЗУ.

2. **Не пиши слишком абстрактно**
   > "Нужно правильно использовать composables"
   > → Покажи конкретный пример кода!

3. **Не дублируй информацию**
   > Если уже есть в одном месте — сделай ссылку

4. **Не игнорируй маленькие открытия**
   > "Это очевидно, не буду записывать"
   > → Через неделю это НЕ будет очевидно
