# 📜 Changelog Management

> Правила ведения истории изменений проекта.

---

## 📁 Файлы Changelog

### 1. Git Changelog (`/project/CHANGELOG.md`)

**Цель:** Техническая история изменений для разработчиков.

| Параметр | Значение |
|----------|----------|
| Язык | English |
| Формат | [Keep a Changelog](https://keepachangelog.com/) |
| Аудитория | Разработчики, AI-агенты |

**Структура:**
```markdown
## [Unreleased]
### Added
- New feature description

### Changed
- Changed behavior description

### Fixed
- Bug fix description

---

## [0.5.0] - 2025-12-20
### Added
...
```

**Когда обновлять:**
- После `feature finish` → добавь в `[Unreleased]`
- После `hotfix finish` → добавь в `[Unreleased]` → `### Fixed`
- При `release finish` → перенеси `[Unreleased]` в новую версию с датой

---

### 2. User Changelog (`frontend/content/changelog.json`)

**Цель:** История изменений для пользователей приложения.

| Параметр | Значение |
|----------|----------|
| Язык | Русский |
| Формат | JSON |
| Аудитория | Пользователи Mini App |

**Структура:**
```json
[
  {
    "version": "0.5.0",
    "date": "2025-12-20",
    "entries": [
      {
        "type": "feature",
        "title": "Новая функция",
        "description": "Понятное описание для пользователя."
      },
      {
        "type": "fix",
        "title": "Исправление",
        "description": "Что было сломано и теперь работает."
      }
    ]
  }
]
```

**Типы записей:**
| type | Иконка | Цвет | Описание |
|------|--------|------|----------|
| `feature` | ✨ Sparkles | Purple | Новая функция |
| `fix` | 🐛 Bug | Orange | Исправление бага |
| `release` | 🚀 Rocket | Green | Важный релиз |

**Что добавлять:**
- ✅ Новые функции, которые видит пользователь
- ✅ Исправления UX/UI багов
- ✅ Улучшения производительности (если заметны)

**Что НЕ добавлять:**
- ❌ Админские функции (API stats, etc.)
- ❌ Рефакторинг кода
- ❌ Технический долг
- ❌ Изменения инфраструктуры
- ❌ Обновления зависимостей

---

## 🔄 Workflow

### После Feature Finish

1. Обнови `project/CHANGELOG.md`:
   ```markdown
   ## [Unreleased]
   ### Added
   - New search algorithm for better movie matching
   ```

2. Если user-facing, обнови `frontend/content/changelog.json`:
   ```json
   {
     "type": "feature",
     "title": "Улучшенный поиск",
     "description": "Теперь фильмы находятся быстрее и точнее."
   }
   ```

### После Release Finish

1. В `project/CHANGELOG.md`:
   - Замени `[Unreleased]` на `[X.Y.Z] - YYYY-MM-DD`
   - Создай новый пустой `[Unreleased]`

2. В `frontend/content/changelog.json`:
   - Все записи уже должны быть с правильной версией

---

## 📍 Где находится страница

- **Путь:** Профиль → "История изменений"
- **URL:** `/changelog`
- **Компонент:** `frontend/pages/changelog.vue`
