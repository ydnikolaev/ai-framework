# 📝 Правила написания Markdown документации

Этот гайд описывает стандарты оформления `.md` файлов в проекте.

## 1. Структура файла

Каждый файл документации должен начинаться с заголовка H1 и краткого описания.

```markdown
# Название документа

Краткое описание того, о чем этот документ (1-2 предложения).

## Раздел 1
...
```

## 2. Заголовки

- Используйте `ATX` стиль (через `#`).
- Соблюдайте иерархию:
  - `# H1` — только один раз в начале файла.
  - `## H2` — основные разделы.
  - `### H3` — подразделы.
- Не пропускайте уровни (после `##` не должно идти сразу `####`).

## 3. Блоки кода

Всегда указывайте язык для подсветки синтаксиса.

````markdown
```go
func main() {
    fmt.Println("Hello")
}
```
````

Для команд терминала используйте `bash` или `sh`:

````markdown
```bash
make dev
```
````

## 4. Ссылки

- Используйте **относительные ссылки** для файлов внутри репозитория: `[Link](../folder/file.md)`.
- Для внешних ресурсов — полные URL.

## 5. Выделение

- **Жирный** (`**text**`) — для акцентов и названий UI элементов.
- *Курсив* (`*text*`) — для терминов или легкого выделения.
- `Код` (backticks) — для путей файлов, переменных, названий функций, классов.

## 6. Списки

- Используйте дефис `-` для маркированных списков.
- Используйте цифры `1.` для нумерованных.

## 7. Alerts (GitHub Style)

Используйте стандартные блоки предупреждений GitHub:

```markdown
> [!NOTE]
> Полезная информация или контекст.

> [!TIP]
> Совет или best practice.

> [!IMPORTANT]
> Важная информация, которую нельзя пропустить.

> [!WARNING]
> Предупреждение о возможных проблемах.

> [!CAUTION]
> Опасные действия (потеря данных и т.д.).
```

## 8. Таблицы

Используйте для структурированных данных. Форматируйте столбцы для читаемости в исходном коде.

| Функция | Описание |
| :--- | :--- |
| `SaveUser` | Сохраняет пользователя в БД |
| `GetMovie` | Получает фильм по ID |

## 9. Emoji

Используйте умеренно для улучшения навигации:
- 📂 Папки
- 📄 Файлы
- ⚠️ Предупреждения
- ✅ Успех
- ❌ Ошибка
