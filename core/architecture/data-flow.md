# 📊 Data Flow

> Визуализация потоков данных между компонентами системы.

---

## 🔄 Общий Flow

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Telegram  │────▶│   Backend   │────▶│  Database   │
│  Mini App   │◀────│   (Go API)  │◀────│ (PostgreSQL)│
└─────────────┘     └─────────────┘     └─────────────┘
                           │
                           ▼
                    ┌─────────────┐
                    │ External API│
                    │ (AI, 3rd)   │
                    └─────────────┘
```

---

## 📱 Frontend → Backend

### HTTP Request Flow

```
1. User Action (click, submit)
         │
         ▼
2. Composable (useFeatures, useData)
         │
         ▼
3. $fetch('/api/endpoint', {
     headers: { Authorization: initData }
   })
         │
         ▼
4. Backend Handler
   - Parse request
   - Validate initData
   - Extract user from context
         │
         ▼
5. Service Layer
   - Business logic
   - External API calls
         │
         ▼
6. Repository Layer
   - SQL query
   - Return data
         │
         ▼
7. Response → Frontend
```

---

## 🔐 Authentication Flow

```
┌─────────────┐
│   Telegram  │
│   Client    │
└──────┬──────┘
       │ 1. Open Mini App
       ▼
┌─────────────┐
│   Frontend  │
│   (Nuxt)    │
└──────┬──────┘
       │ 2. useTelegram() → get initData
       ▼
┌─────────────┐
│   API Call  │ headers: { Authorization: initData }
└──────┬──────┘
       │ 3. Send request
       ▼
┌─────────────┐
│  Middleware │ 4. ValidateInitData(initData, botToken)
└──────┬──────┘
       │ 5. Parse user from initData
       ▼
┌─────────────┐
│   Handler   │ 6. ctx.Value("user") → User
└─────────────┘
```

---

## 💾 State Management

```
┌─────────────────────────────────────────┐
│              Frontend State             │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────┐    ┌─────────────┐    │
│  │ useTelegram │───▶│ Global Refs │    │
│  │ (singleton) │    │ user, auth  │    │
│  └─────────────┘    └─────────────┘    │
│                                         │
│  ┌─────────────┐    ┌─────────────┐    │
│  │ useFeature  │───▶│ Component   │    │
│  │ (per-page)  │    │ Local State │    │
│  └─────────────┘    └─────────────┘    │
│                                         │
└─────────────────────────────────────────┘
```

---

## 🔄 Real-time Updates (Future)

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  Client A   │     │   Backend   │     │  Client B   │
└──────┬──────┘     └──────┬──────┘     └──────┬──────┘
       │                   │                   │
       │ 1. Action         │                   │
       │──────────────────▶│                   │
       │                   │ 2. Broadcast      │
       │                   │──────────────────▶│
       │                   │                   │
       │ 3. WebSocket      │                   │
       │◀──────────────────│                   │
       │ Update UI         │                   │
```
