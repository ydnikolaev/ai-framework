# 🧹 Правила линтинга (ESLint)

## 1. Философия
Мы используем официальный модуль `@nuxt/eslint`. Он автоматически генерирует правила, совместимые с Vue 3, Nuxt 4 и TypeScript.

## 2. Команды

| Команда | Описание |
|---------|----------|
| `npm run lint` | Проверка кода (без изменений) |
| `npm run lint:fix` | Автоматическое исправление ошибок |
| `npm run typecheck` | Проверка типов TypeScript (vue-tsc) |

## 3. Конфигурация
Файл конфигурации: `frontend/eslint.config.mjs`

### Основные настройки:
- **Vue**: Отключены правила, мешающие быстрой разработке (например, multi-word-component-names).
- **TypeScript**:
  - `no-explicit-any`: **OFF** (для совместимости с легаси кодом).
  - `no-unused-vars`: **WARN** (чтобы не ломать сборку при разработке).
- **Console**: `warn` в продакшене, разрешено в разработке.

```javascript
import withNuxt from './.nuxt/eslint.config.mjs'

export default withNuxt({
  files: ['**/*.ts', '**/*.vue', '**/*.js', '**/*.mjs'],
  rules: {
    'vue/multi-word-component-names': 'off',
    '@typescript-eslint/no-explicit-any': 'off',
    '@typescript-eslint/no-unused-vars': 'warn'
  }
})
```

## 4. Проверка Типов (Typecheck)
Мы используем встроенную в Nuxt проверку типов (через `vue-tsc`).
- **В разработке**: Включено автоматическое выполнение при `npm run dev` (опция `typescript: { typeCheck: true }` в `nuxt.config.ts`).
- **Ручной запуск**: `npm run typecheck`.

## 5. Игнорирование
ESLint автоматически уважает `.gitignore`.
Дополнительные исключения можно добавить в `eslint.config.mjs` через `ignores: []`.
