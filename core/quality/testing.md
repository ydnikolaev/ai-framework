# 🧪 Правила Тестирования

Роль: Ты — QA Engineer и разработчик, который верит в то, что "непротестированный код — это легаси".

## 1. ФИЛОСОФИЯ: "Пирамида Тестирования"

Мы придерживаемся разумного баланса:
1.  **Unit Tests (70%):** Быстрые, изолированные тесты функций и методов.
2.  **Integration Tests (20%):** Тесты взаимодействия с БД и внешними API (в контейнерах).
3.  **E2E (10%):** Проверка ключевых сценариев пользователя (редко, т.к. дорого поддерживать).

---

## 2. BACKEND (Go)

Используем стандартный `testing` пакет + `testify` для ассертов.

### А. Table-Driven Tests
Это стандарт де-факто в Go.

```go
func TestSum(t *testing.T) {
    tests := []struct {
        name string
        a, b int
        want int
    }{
        {"positive", 2, 3, 5},
        {"negative", -1, -1, -2},
    }
    
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            got := Sum(tt.a, tt.b)
            assert.Equal(t, tt.want, got)
        })
    }
}
```

### Б. Моки (Mocks)
Для изоляции слоев (Service не должен лезть в реальную БД при юнит-тестах) используем интерфейсы и генерацию моков.
Инструмент: `mockery` или рукописные моки (для простых случаев).

### В. Integration Tests (с БД)
Для тестов репозитория используем **настоящую** базу в Docker.
- Поднимаем чистый Postgres (или используем `testcontainers`).
- Накатываем миграции.
- Прогоняем тесты.
- Чистим данные (транзакция с откатом или `TRUNCATE`).

---

## 3. FRONTEND (Nuxt / Vue)

Используем **Vitest** (быстрый аналог Jest для Vite) и **Vue Test Utils**.

### А. Unit Tests (Logic)
Тестируем `composables` и `utils`.

```typescript
// tests/utils.test.ts
import { describe, it, expect } from 'vitest'
import { formatDate } from '../utils/format'

describe('formatDate', () => {
  it('formats correctly', () => {
    expect(formatDate('2025-01-01')).toBe('01.01.2025')
  })
})
```

### Б. Component Tests
Проверяем рендеринг и события.

```typescript
import { mount } from '@vue/test-utils'
import MyButton from '../components/MyButton.vue'

test('emits click', async () => {
  const wrapper = mount(MyButton)
  await wrapper.trigger('click')
  expect(wrapper.emitted()).toHaveProperty('click')
})
```

---

## 4. КОМАНДЫ (Make)

Добавь эти команды в `Makefile` своего проекта:

```makefile
test-backend:
	cd backend && go test -v ./...

test-frontend:
	cd frontend && npm run test

test-all: test-backend test-frontend
```

## 5. ЧЕК-ЛИСТ (Перед PR)
- [ ] Тесты проходят локально (`make test-all`)?

## 6. ОТЧЕТЫ
Результаты ручного тестирования и отчеты складывай в `/project/testing/`.
