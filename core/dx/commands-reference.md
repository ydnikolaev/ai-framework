# ⚡ Шпаргалка Команд (Human Readable)

Этот файл — твой быстрый помощник. Здесь собраны все команды, которые нужны для ежедневной работы и отладки.

---

## 💻 Локальная разработка (Mac)

Эти команды выполняются в терминале твоего компьютера, в корне проекта.

### Запуск и Старт
| Команда | Что делает |
|---------|------------|
| `make db` | Запускает базы данных (Postgres + Redis) в фоне. |
| `make frontend` | Запускает Nuxt (сайт) с флагом `--host` (доступен для туннеля). |
| `make api` | Запускает Go бэкенд сервер (REST API). |
| `make bot` | Запускает Telegram бота (использует `TELEGRAM_BOT_TOKEN` из `.env`). |

### Работа с Базой Данных
| Команда | Что делает | Внимание |
|---------|------------|----------|
| `make migrate-dev` | Применяет новые миграции к локальной БД. | Делай это, если изменил структуру таблиц. |
| `make reset-db` | **Полный сброс!** Удаляет все данные и пересоздает БД с нуля. | ⚠️ Уничтожит все локальные данные! |
| `make update-dev` | Запускает скрипт обновления метаданных (TMDB) локально. | |

### Туннель (Доступ извне)
| Команда | Что делает |
|---------|------------|
| `make tunnel`* | Запускает SSH туннель, чтобы `dev.project.ru` вел на твой `localhost:3000`. |
| *или `ssh -R ...` | Если алиас `make tunnel` не настроен, используй полную команду SSH (см. ниже). |

### 🎨 DX Автоматизация (iTerm2)
| Команда | Что делает |
|---------|------------|
| `make dev` | Базовая разработка (2x2 grid: bot, api, frontend, tunnel). |
| `make dev-full` | **Полная разработка** — 2 вкладки: Local (3x2) + Prod (2x2). |
| `make dev-stop` | **Остановить ВСЁ** dev-окружение (bot, api, frontend, tunnel). |
| `make dev-restart` | Перезапустить dev-окружение (stop → start). |
| `make bot-stop` | Остановить все локальные процессы бота. |
| `make prod-watch` | Мониторинг прода (2x2 grid с SSH логами). |
| `make prod-status` | Быстрый статус контейнеров прода. |
| `make prod-logs-api` | Логи API прода. |
| `make prod-logs-bot` | Логи бота прода. |

## 🚀 Сервер и Деплой (VPS)

### Подключение
```bash
# Зайти на сервер
ssh deploy@your-server-ip
```

### Просмотр Логов (Самое важное!)
Когда ты на сервере (`ssh deploy@...`), используй эти команды для отладки:

| Команда | Описание |
|---------|----------|
| `docker logs project_api --tail 100 -f` | **Логи Бэкенда (API)**. Покажет последние 100 строк и будет следить за новыми. Жми `Ctrl+C` для выхода. |
| `docker logs project_bot --tail 100 -f` | **Логи Бота**. Ошибки телеграма, ответы AI и т.д. |
| `docker logs project_frontend --tail 100` | Логи Фронтенда (обычно там мало интересного, если SSR выключен). |
| `docker logs traefik --tail 50` | Логи Прокси. Смотри сюда, если сайт выдает "Bad Gateway" или проблемы с SSL. |

### Управление Контейнерами (Prod)
Перейди в папку проекта: `cd ~/project-name`

| Команда | Что делает |
|---------|------------|
| `docker compose -f docker-compose.prod.yml restart api` | Перезагрузить только API (быстро). |
| `docker compose -f docker-compose.prod.yml restart bot` | Перезагрузить только Бота. |
| `docker compose -f docker-compose.prod.yml up -d --build` | Пересобрать и перезапустить всё (как при деплое). |

### Миграции на Проде
Лучше делать через Makefile с локальной машины (если настроен туннель к БД) или через Docker внутри сервера.

**Способ с локальной машины (безопаснее):**
```bash
make migrate-prod
```
*(Требует правильного `DATABASE_URL_PROD` в локальном `.env`)*

---

## 🛠 Полезные Алиасы для Терминала (zsh)

Добавь это в свой `~/.zshrc` на Маке, чтобы чувствовать себя хакером:

```bash
# Быстрый туннель
alias dev-tunnel="ssh -R 31337:localhost:3000 deploy@your-server-ip -N"

# Быстро зайти на сервер
alias ssh-prod="ssh deploy@your-server-ip"

# Посмотреть логи API одной командой (с локального компа!)
alias logs-api="ssh deploy@your-server-ip 'docker logs project_api --tail 100 -f'"

# Посмотреть логи Бота
alias logs-bot="ssh deploy@your-server-ip 'docker logs project_bot --tail 100 -f'"
```

Теперь ты можешь просто писать `logs-api` и сразу видеть, что происходит на проде! 🎩✨
