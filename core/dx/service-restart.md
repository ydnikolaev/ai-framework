# 🔄 Когда перезапускать сервисы

> Справочник для локальной разработки.

---

## 🎯 Quick Reference

| Изменил | Перезапустить |
|---------|---------------|
| `backend/internal/bot/**` | **Bot** (`make bot`) |
| `backend/internal/api/**` | **API** (`make api`) |
| `backend/cmd/**` | **Bot** или **API** (зависит от файла) |
| `frontend/**/*.vue` | ✅ Hot Reload (автоматически) |
| `frontend/**/*.ts` (composables) | ✅ Hot Reload (обычно автоматически) |
| `frontend/nuxt.config.ts` | **Frontend** (`make front`) |
| `frontend/tailwind.config.js` | **Frontend** (`make front`) |
| `.env` | **Все сервисы** (Bot + API + Frontend) |
| `migrations/**` | Запустить `make migrate` |

---

## 🤖 Bot (Telegram Bot)

**Перезапустить когда:**
- Изменили обработчики сообщений (`handler.go`)
- Изменили логику AI (`ai/service.go`)
- Изменили клиенты API (`tmdb/`, `kinopoisk/`)
- Добавили новые команды

**Команда:** `make bot` или Ctrl+C → `make bot`

---

## 🌐 API (HTTP Server)

**Перезапустить когда:**
- Изменили endpoints (`api/server.go`)
- Изменили middleware (`api/auth.go`)
- Изменили repository (`repository/*.go`)

**Команда:** `make api` или Ctrl+C → `make api`

---

## 💻 Frontend (Nuxt)

**Авто-перезагрузка (Hot Reload):**
- Изменения в `.vue` файлах
- Изменения в composables
- Изменения в components

**Требует перезапуска:**
- `nuxt.config.ts`
- `tailwind.config.js`
- `package.json` (после `npm install`)
- Изменения в `plugins/`

**Команда:** `make front` или Ctrl+C → `make front`

---

## 🌐 Tunnel (SSH)

**Перезапустить когда:**
- Туннель упал (проверить `make tunnel`)
- Поменял порт в `.env`

**Команда:** `make tunnel`

---

## 📝 Примечания для AI

После редактирования кода бэкенда, **ОБЯЗАТЕЛЬНО** напомни пользователю:

```
⚠️ Перезапусти бота: `make bot`
```

или

```
⚠️ Перезапусти API: `make api`
```

Frontend обычно обновляется автоматически благодаря Hot Module Replacement (HMR).
