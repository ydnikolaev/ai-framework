# 🤖 AI Documentation Generation SOP

> **Standard Operating Procedure** для AI-агентов при создании и редактировании документации.

## 🚨 Critical Rules (Zero Tolerance)

Если ты нарушаешь эти правила — документация сломается у пользователя.

### 1. Mermaid Диаграммы

Самая частая причина поломки документации.

**✅ DO:**
1.  Всегда используй `graph TD` (самый совместимый тип).
2.  **ОБЯЗАТЕЛЬНО** бери в кавычки любой текст с пробелами, кириллицей или спецсимволами (`?`, `!`, `()`).
    - `A -- "Да, это так" --> B`
    - `Node["Русский текст"]`
3.  Проверяй валидность синтаксиса (нет висячих стрелок, закрытые скобки).

**❌ DON'T:**
1.  Никаких `flowchart TD` (не везде рендерится).
2.  Никакого текста без кавычек, если это не `id` узла на латинице.
    - `A -- Русский текст --> B` (СЛОМАЕТСЯ)
3.  Не используй сложные стили (`style A fill:#f9f`), если не просили.

### 2. Форматирование Markdown

**✅ DO:**
1.  Всегда указывай язык кода: ```` ```go ````, ```` ```mermaid ````.
2.  Используй GitHub Alerts для важных заметок (`> [!WARNING]`).
3.  Используй относительные ссылки на файлы: `[link](../core/test.md)`.

**❌ DON'T:**
1.  Не оставляй `TODO` или плейсхолдеры (`...`) в финальной версии, если это не шаблон.
2.  Не используй абсолютные пути в ссылках.

### 3. Проверка качества

Перед тем как сказать "Готово":
1.  Проверь файл на невидимые символы (Zero Width Space), если копировал из чата.
2.  Убедись, что ссылки ведут на существующие файлы.
3.  Если обновляешь файл — сохрани структуру заголовков.
