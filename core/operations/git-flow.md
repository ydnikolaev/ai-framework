# 🌿 Git Flow — Правила работы с ветками

> Стандартный workflow для безопасной разработки.

---

## 📋 Ветки

| Ветка | Назначение | CI/CD |
|-------|------------|-------|
| `main` | Продакшен | ✅ Автодеплой |
| `dev` | Стабильная разработка | 🔄 Тесты |
| `feature/*` | Новые фичи | 🔄 Тесты |
| `fix/*` | Исправления багов | 🔄 Тесты |
| `hotfix/*` | Срочные фиксы прода | ✅ Автодеплой |

---

## 🔄 Workflow

### Обычная разработка

```
main ────────────────────────────●──────────
                                 ↑
dev ───────●────●────●──────────●───────────
           ↑    ↑    ↑ merge
feature/X ─●────●────●
```

1. **Создай ветку от `dev`:**
   ```bash
   git checkout dev
   git pull origin dev
   git checkout -b feature/add-filters
   ```

2. **Работай и коммить:**
   ```bash
   git add .
   git commit -m "feat: add genre filter"
   git push origin feature/add-filters
   ```

3. **Merge в `dev`** (когда готово):
   ```bash
   git checkout dev
   git merge feature/add-filters
   git push origin dev
   ```

4. **Релиз в `main`** (только владелец):
   ```bash
   git checkout main
   git merge dev
   git push origin main  # Triggers deploy
   ```

---

## 🤖 Правила для AI-агента

### ✅ МОЖНО самостоятельно:
- Коммитить и пушить в `ai-framework` (отдельный репозиторий)
- Коммитить и пушить в `dev`, `feature/*`, `fix/*`
- Создавать Pull Request из `dev` в `main`

### ❌ НЕЛЬЗЯ без команды пользователя:
- Пушить напрямую в `main` проекта
- Мержить PR в `main`
- Делать force push

---

## 🏷️ Commit Messages

Формат: `<type>: <description>`

| Тип | Описание |
|-----|----------|
| `feat` | Новая фича |
| `fix` | Исправление бага |
| `docs` | Документация |
| `style` | Форматирование (не влияет на логику) |
| `refactor` | Рефакторинг без изменения поведения |
| `test` | Тесты |
| `chore` | Рутина (deps, configs) |

Примеры:
```
feat: add genre filter to movie list
fix: poster image flash on load
docs: add git flow to framework
chore: update dependencies
```

---

## 🚀 Quick Reference

```bash
# Начать фичу
git checkout dev && git pull
git checkout -b feature/my-feature

# Коммит
git add . && git commit -m "feat: description"

# Пуш в фичу
git push origin feature/my-feature

# Merge в dev (сам или через PR)
git checkout dev && git merge feature/my-feature && git push origin dev

# Релиз (ТОЛЬКО владелец)
git checkout main && git merge dev && git push origin main
```

---

## ⚠️ Важно

- **Никогда не делай force push** в `main` или `dev`
- **Всегда делай pull** перед началом работы
- **`main` = продакшен** — только стабильный код
- **`dev` = staging** — можно ломать, но чини быстро
