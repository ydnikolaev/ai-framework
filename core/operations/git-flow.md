# 🌿 Git Flow — Правила работы с ветками

> Стандартный workflow с использованием **git-flow CLI**.

---

## 📋 Ветки

| Ветка | Назначение |
|-------|------------|
| `main` | **Production**. Только релизные теги. |
| `dev` | **Development**. Слияние всех фич. |
| `feature/*` | Разработка новой функциональности. |
| `hotfix/*` | Срочные исправления багов на проде. |
| `release/*` | Подготовка релиза (финализация, версионирование). |

---

## 🤖 Правила для AI-агента

> 🛑 **CRITICAL RULE #1:** Никогда не делай `finish` (merge) ветки без явного подтверждения пользователя!
> Сначала — тесты. Потом — merge.

> 🛑 **CRITICAL RULE #2:** Никогда не делай `commit` без явной команды пользователя!
> Пользователь должен сказать "коммит" или "commit" — только тогда выполняй `git add && git commit`.
> Самостоятельные коммиты запрещены.

AI-агент обязан использовать утилиту `git flow` для управления ветками.

### 1. Новая функциональность (Feature)
**Триггер:** "Сделай новую фичу", "Реализуй...", "Добавь..."
1.  **Start:** `git flow feature start <feature_name>`
    *   *Пример:* `git flow feature start smart-search`
2.  **Work:** Изменяем код, коммитим (`git add`, `git commit`).
    *   **Одна фича = несколько коммитов.** Фича — это логически завершенная единица работы (например, целая страница), внутри которой может быть много промежуточных коммитов.
3.  **Test:** ✋ Сообщи пользователю: "Готово к тестам".
    *   **НЕ ЗАКРЫВАЙ ВЕТКУ** пока пользователь не скажет "Всё ок, сливай".
4.  **Finish:** `git flow feature finish <feature_name>` (Только после команды!)
    *   *Действие:* Мержит в `dev`, удаляет ветку фичи.
5.  **Push:** `git push origin dev`

> **📝 Терминология команд пользователя:**
> - **"Коммит" / "Commit"** → Сделать `git add . && git commit` **внутри текущей ветки**. НЕ мержить, НЕ закрывать ветку.
> - **"Закончи ветку" / "Закончи фичу" / "Finish"** → Сделать `git flow feature finish`. Мержит в `dev`.

### 2. Исправление багов (Fix)
**Триггер:** "Исправь баг в разработке"
*   Используй `feature` workflow.
*   **Start:** `git flow feature start fix-<bug_name>`
*   **Work:** Фиксим -> Коммитим.
*   **Test:** ✋ Ждем подтверждения пользователя.
*   **Finish:** `git flow feature finish fix-<bug_name>` (Только после аппрува!)

### 3. Исправление продакшена (Hotfix)
**Триггер:** "Баг на проде", "Срочный фикс"
1.  **Start:** `git flow hotfix start <version>` (e.g. `0.4.1`)
2.  **Work:** Фиксим баг.
3.  **Changelog:** Обнови `project/CHANGELOG.md` (добавь в раздел hotfix версии).
4.  **Finish:** `git flow hotfix finish <version>`
    *   *Действие:* Мержит в `master` и `dev`, ставит тег.
5.  **Push:** `git push origin master dev --tags`

---

## 📝 Обновление Changelog

> **ВАЖНО:** После каждой фичи/фикса/релиза обновляй соответствующие changelog файлы!

### Git Changelog (`/project/CHANGELOG.md`)
- **Язык:** English
- **Формат:** [Keep a Changelog](https://keepachangelog.com/)
- **Когда обновлять:**
  - После `feature finish` → добавь в `[Unreleased]` → `### Added` или `### Changed`
  - После `fix finish` → добавь в `[Unreleased]` → `### Fixed`
  - После `release finish` → перенеси `[Unreleased]` в новую версию с датой

### User Changelog (`frontend/content/changelog.json`)
- **Язык:** Русский
- **Аудитория:** Пользователи приложения
- **Что добавлять:** Только user-facing изменения (новые функции, исправления UX)
- **Что НЕ добавлять:** Админские фичи, рефакторинг, технический долг

### 4. Релиз (Release)
**Триггер:** "Готовим релиз", "Выкати обновление"
1.  **Start:** `git flow release start <version>` (e.g. `0.5.0`)
2.  **Docs:**
    *   Обнови `project/CHANGELOG.md` (внеси дату релиза).
    *   Обнови версию в `project/CONFIG.yaml` (или `package.json`).
3.  **Commit:** `git commit -am "chore: bump version to <version>"`
4.  **Finish:** `git flow release finish -m "Release <version>" <version>`
5.  **Push:** `git push origin master dev --tags`

---

## 🏷️ Commit Messages (Conventional Commits)

Формат: `<type>: <description>`

| Тип | Описание |
|-----|----------|
| `feat` | Новая фича |
| `fix` | Исправление бага |
| `docs` | Документация |
| `style` | Форматирование (не влияет на логику) |
| `refactor` | Рефакторинг без изменения поведения |
| `test` | Тесты |
| `chore` | Рутина (deps, configs) |

---

## 🚀 Quick Reference (CLI)

```bash
# Инициализация (один раз)
git flow init -d

# Фичи
git flow feature start my-feature
# ... work ...
# ✋ STOP: Просим пользователя протестировать!
git flow feature finish my-feature
git push origin dev

# Релизы
git flow release start 1.0.0
# ... update version/changelog ...
git flow release finish -m "1.0.0" 1.0.0
git push origin master dev --tags
```

---

## ⚠️ Важно

- **🛑 Жди подтверждения от пользователя перед `finish` (слиянием)!**
- **Никогда не делай force push** в `main` или `dev`.
- **Всегда делай pull** перед началом работы.
