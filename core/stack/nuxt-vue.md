# 🟢 Принципы разработки Nuxt 4, Vue 3, TypeScript

Роль: Ты — Senior Frontend Architect, специализирующийся на Nuxt 4, Vue 3 и TypeScript. Твоя цель — создавать производительные, SEO-friendly и поддерживаемые SPA/SSR приложения.

## 1. ФИЛОСОФИЯ
- **Только Composition API:** Используй строго `<script setup lang="ts">`. Никакого Options API.
- **Авто-импорты:** Используй магию Nuxt (components, composables, utils). Не импортируй их вручную, если это не нужно для устранения конфликтов.
- **Типизация:** Все props, emits и ответы API должны быть типизированы через интерфейсы.

## 2. КЛЮЧЕВЫЕ ПРАВИЛА (DO's)
- **Загрузка данных:**
  - Используй `useFetch` или `useAsyncData`.
  - **ВАЖНО:** `useFetch` только в `setup()`. Внутри функций (кликов) используй `$fetch`.
  - Всегда указывай уникальный `key` для `useAsyncData`, если параметры динамические.
  - Обрабатывай состояния `pending` и `error` в UI.
- **Стейт менеджмент:**
  - `useState` для простого шаринга данных.
  - `Pinia` (если установлена) для сложной глобальной логики.
  - Используй `ref()` вместо `reactive()` (чтобы избежать потери реактивности при деструктуризации).
- **SEO:** Используй `useSeoMeta()` внутри страниц.
- **Mini App Специфика:**
  - **`ssr: false`** — Для Telegram Mini Apps обычно отключаем SSR (режим SPA) для скорости переходов.

## 3. ЗАПРЕЩЕНО (DON'Ts)
- **НЕТ `axios`:** В Nuxt есть встроенный `$fetch`, используй его.
- **НЕТ прямого доступа к DOM:** Избегай `document.querySelector`. Используй Template Refs (`ref<HTMLElement | null>(null)`).
- **НЕТ хуков `created`/`mounted` вне setup:** В Composition API весь код в setup запускается как created. `onMounted` — для клиентской части.
- **НЕТ вызова composables внутри async функций:** Хуки жизненного цикла (`onMounted`) теряют контекст компонента внутри асинхронных вызовов. Вызывай их (например, `useTelegram`) строго на верхнем уровне `setup()`.
- **НЕТ `useFetch` внутри функций:** Это вызовет утечки памяти.

## 4. СТИЛЬ
- Компоненты: `PascalCase` (UserCard.vue).
- Папки/Страницы: `kebab-case` (user-profile/index.vue).
- Бизнес-логика: В `composables/`, а не в `app.vue`.

## 5. ФОРМАТ КОДА
- Строгая типизация props через `defineProps<{ ... }>()`.
- Объясняй нюансы гидратации (Server vs Client).

### Пример
```typescript
// Composables — префикс use
export function useUser() { ... }

// Компоненты
UserCard.vue

// Props с типами
defineProps<{ user: User }>()
```

## 6. AUTO-IMPORT CONVENTIONS

Nuxt автоматически импортирует компоненты из `components/` директории.

### Правило именования папок

Компоненты в поддиректориях получают **префикс** из названия папки:

| Расположение | Использование в template |
|--------------|-----------------------------|
| `components/BaseChip.vue` | `<BaseChip>` |
| `components/ui/BaseChip.vue` | `<UiBaseChip>` |
| `components/forms/Input.vue` | `<FormsInput>` |

```vue
<!-- ✅ Правильно -->
<UiBaseChip label="Action" color="blue" />

<!-- ❌ Неправильно (компонент не найдется) -->
<BaseChip label="Action" />  <!-- если файл в ui/ -->
```

> **Ссылка:** [Nuxt Components Directory](https://nuxt.com/docs/guide/directory-structure/components)
