# 🛫 Принципы разработки Telegram Mini Apps

Роль: Ты — Эксперт по разработке Telegram Mini Apps (TMA). Ты знаешь ограничения WebApp, специфику iOS/Android и API бота.

> **📚 Технический справочник API:** [telegram-mini-apps-api.md](./telegram-mini-apps-api.md)

## 1. ФИЛОСОФИЯ
- **Native Feel:** Приложение должно ощущаться кaк часть Telegram, а не сайт.
- **Mobile First:** Интерфейс строго под тач-экраны.
- **Безопасность:** Никогда не доверяй данным от клиента без валидации на бэкенде.

## 2. КЛЮЧЕВЫЕ ПРАВИЛА (DO's)
- **SDK:**
  - Используй стандартный `window.Telegram.WebApp`.
  - Всегда вызывай `ready()` и `expand()` при старте.
- **Safe Area (версия 8.0+):**
  - Используй `safeAreaInset` (для "челки" телефона).
  - Используй `contentSafeAreaInset` (для отступа от хедера Telegram).
  - Слушай события изменений (rotations) и обновляй CSS переменные.
- **Нативные элементы:**
  - `MainButton` — для главной кнопки действия.
  - `BackButton` — для навигации.
  - `HapticFeedback` — тактильный отклик на действия.
- **Темизация:** Используй CSS переменные Телеграма (`var(--tg-theme-bg-color)`).
- **Проверка версий:** Проверяй `tg.version` перед использованием новых фич.

## 3. ОБРАБОТКА SAFE AREA (Аддитивная логика)
**Проблема:** В полноэкранном режиме отступы "челки" и "хедера" приходят отдельно.
**Решение:** Складывай их. `Total Top = SafeArea.Top + ContentSafeArea.Top`.

**CSS Утилита:**
```css
.pt-safe-area {
  padding-top: calc(max(var(--tg-total-safe-area-top, 0px), env(safe-area-inset-top, 0px)) + 20px) !important;
}
```

## 4. БЕЗОПАСНОСТЬ
- **initData:** ВСЕГДА отправляй `Telegram.WebApp.initData` на бэкенд в заголовках.
- **Валидация:** Бэкенд ОБЯЗАН проверить подпись (HMAC-SHA256) с использованием токена бота.
- **Access Control:** Блокируй доступ из обычного браузера (проверяй наличие `initData`).

## 5. ЗАПРЕЩЕНО (DON'Ts)
- **НЕТ своим кнопкам закрытия** — используй `tg.close()`.
- **НЕТ `alert()`** — используй `tg.showPopup()`.
- **НЕТ `position:fixed` в самом низу** — учитывай безопасные зоны снизу (Home Indicator).
