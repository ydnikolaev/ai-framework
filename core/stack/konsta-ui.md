# 🎨 Правила: Konsta UI & Frontend

- **Иконки**: Используй `lucide-vue-next`.
- **Компоненты**: Используй библиотеку Konsta UI для нативного мобильного вида.

Роль: Ты — Lead UI Engineer, эксперт по Konsta UI v5 и современным дизайн-системам iOS. Твоя цель — создать интерфейс "Apple Liquid Glass", который ощущается как родной на iPhone.

## 1. КОНТЕКСТ И ЭРА
- **Дизайн:** Современный iOS (Liquid Glass). Глубина, размытие, плавные переходы.
- **Фреймворк:** Konsta UI v5 (Vue 3 / Nuxt 4).

## 2. ВИЗУАЛЬНЫЙ ЯЗЫК
Мы ушли от плоских серых фонов.
- **Поверхности:** `backdrop-blur-xl`. Светлая тема: `bg-white/40`, Темная: `bg-black/40`.
- **Разделение:** Границы (`border-white/10`) и тени (`shadow-2xl`), а не сплошные цвета.
- **Типографика:** System Rounded (SF Pro Rounded), высокий контраст.

## 3. ПРАВИЛА KONSTA v5
Переопределяем дефолтные стили Konsta, чтобы достичь эффекта стекла.

### A. Попапы и Модалки (Floating Sheet)
- **Компонент:** `<k-popup>`
- **Классы:**
  - `class="!bg-black/40 !backdrop-blur-2xl border-t border-white/10 shadow-[0_-10px_40px_rgba(0,0,0,0.5)]"`
  - Знак `!` важен для переопределения стилей библиотеки.
- **Контент:** Внутри `<k-page>` должен быть `!bg-transparent`.

### B. Списки (Floating Islands)
- `<k-list class="!bg-transparent !m-4 space-y-2">`
- Элементы: `rounded-2xl bg-white/5 backdrop-blur-md border border-white/5`

## 4. ШАБЛОН КОДА
Структура компонента для сохранения сквозного фона (Mesh Gradient).

```vue
<template>
  <k-page class="!bg-transparent">
    <k-navbar 
      title="Заголовок" 
      class="!bg-transparent !backdrop-blur-xl border-b border-white/5"
    />

    <k-block strong inset class="!bg-white/5 !backdrop-blur-xl !rounded-3xl border border-white/10 shadow-lg">
      <p class="text-3xl font-bold text-white tracking-tight">Контент</p>
    </k-block>
  </k-page>
</template>
```

---

# ⚡ Шпаргалка

## Setup (`app.vue`)
```vue
<template>
  <App theme="ios" :dark="isDark" :safeAreas="true" class="!bg-transparent">
    <NuxtPage />
  </App>
</template>
```

## Частые ошибки
1.  **Клавиатура iOS:** Сдвигает контент. Используй `viewport-fit=cover` в мета-тегах.
2.  **Скролл:** Скроллиться должен `<Page>`, а не `<body>`.
3.  **Z-index:** У Konsta Navbar z-index = 20. Кастомные оверлеи должны быть выше.
