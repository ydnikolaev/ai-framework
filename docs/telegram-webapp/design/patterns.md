# 🎨 Telegram Mini Apps Design Patterns

> **Goal:** Create apps that feel like a native part of Telegram.

---

## 📱 Core Principles

1.  **Mobile-First:** Дизайн должен быть адаптивным, тестируй на телефоне.
2.  **Native Feel:** Элементы должны вести себя как нативные (плавные анимации, свайпы).
3.  **To-The-Point:** Минимум навигации, фокус на одной задаче.

---

## 🎨 Color Schemes

Используй CSS-переменные Telegram, чтобы приложение автоматически подстраивалось под тему пользователя (светлая/тёмная/цветная).

### Backgrounds
- `var(--tg-theme-bg-color)` — Основной фон
- `var(--tg-theme-secondary-bg-color)` — Фон списков/карточек
- `var(--tg-theme-header-bg-color)` — Фон хедера

### Text
- `var(--tg-theme-text-color)` — Основной текст
- `var(--tg-theme-hint-color)` — Подсказки/вторичный текст
- `var(--tg-theme-link-color)` — Ссылки
- `var(--tg-theme-button-text-color)` — Текст на кнопках

### Buttons
- `var(--tg-theme-button-color)` — Основная кнопка (Brand color)

➡️ *Полные дизайн-токены см. в [Design System](../../../core/design/design-system.md)*

> **💡 Совет:** Если используешь Tailwind, настрой конфиг на использование этих переменных.

---

## 📐 Layout

- **Safe Areas:** Учитывай "чёлку" и закругления экрана.
  - `var(--tg-viewport-height)` — Реальная высота вьюпорта
  - `var(--tg-safe-area-inset-top)` — Отступ сверху (status bar)
  - `var(--tg-safe-area-inset-bottom)` — Отступ снизу (home indicator)

- **Bottom Sheets:** Для меню и деталей используй шторки (как в нативном Telegram), а не новые экраны.
- **Micro-animations:** 60fps анимации на открытие/закрытие.

---

## ⚡ Haptics (Тактильность)

Пользователь должен **чувствовать** интерфейс.

- **Impact:** При клике на кнопки (Light/Medium).
- **Notification:** При успехе (Success) или ошибке (Error).
- **Selection:** При скролле списков/пикеров (Selection).

➡️ *Подробнее см. [features/haptics.md](../features/haptics.md)*

---

## 🖼️ UI Components

- **Loader:** Используй стандартный Telegram Loader (`WebApp.MainButton.showProgress()`) или похожий дизайн.
- **Main Button:** Большая кнопка внизу экрана (`WebApp.MainButton`). Используй её для главного действия (Save, Pay, Confirm).
