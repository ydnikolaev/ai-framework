# Haptic Feedback (Тактильный отклик)

> **Маст-хэв для UX.** Заставляет телефон вибрировать при взаимодействии.

---

## 💥 Impact (Удары)

Легкая вибрация, имитирующая физическое нажатие.

```typescript
// WebApp.HapticFeedback.impactOccurred(style)
WebApp.HapticFeedback.impactOccurred('light')  // Клик по кнопке, свитчер, чекбокс
WebApp.HapticFeedback.impactOccurred('medium') // Открытие меню, карточки
WebApp.HapticFeedback.impactOccurred('heavy')  // Удаление, важное действие
```

## 🔔 Notification (Уведомления)

Паттерны вибрации для событий.

```typescript
// WebApp.HapticFeedback.notificationOccurred(type)
WebApp.HapticFeedback.notificationOccurred('success') // ✅ Успех (платеж, сохранение)
WebApp.HapticFeedback.notificationOccurred('warning') // ⚠️ Важное предупреждение
WebApp.HapticFeedback.notificationOccurred('error')   // ❌ Ошибка (неверный ввод)
```

## 🎡 Selection (Выбор)

Тонкий отклик при скролле (как в нативном пикере iOS).

```typescript
// WebApp.HapticFeedback.selectionChanged()
// Вызывать при каждом изменении значения в скроллере/слайдере
WebApp.HapticFeedback.selectionChanged()
```

## 💡 Best Practices

1. **Не злоупотребляй:** Вибрация должна подтверждать действие, а не раздражать.
2. **Синхронизируй с анимацией:** Вибрация должна быть ровно в момент клика/изменения.
3. **Selection:** Используй только для перебора значений (колесо удачи, выбор даты).
