# Sensors (Сенсоры)

> **Гироскоп и Акселерометр для креативных интерфейсов.**

Можно использовать для:
- Параллакс-эффектов
- Управления в играх
- Интерактивности при наклоне телефона

---

## 🏎️ Accelerometer

Измеряет ускорение устройства (включая гравитацию).

```typescript
const am = WebApp.Accelerometer

am.start({ refresh_rate: 1000 }, (success) => {
  if (success) {
    console.log('Accelerometer started')
  }
})

// Подписка на данные
// x, y, z - ускорение по осям
am.on('change', (data) => {
  console.log(`X: ${data.x}, Y: ${data.y}, Z: ${data.z}`)
})
```

## 🧭 Gyroscope

Измеряет скорость вращения устройства.

```typescript
const gm = WebApp.Gyroscope

gm.start({ refresh_rate: 1000 }, (success) => {
  if (success) console.log('Gyroscope started')
})

gm.on('change', (data) => {
  console.log(`Alpha: ${data.alpha}, Beta: ${data.beta}, Gamma: ${data.gamma}`)
})
```

## ⚠️ Важно

1. **Батарея:** Сенсоры быстро сажают батарею. Выключай их, когда не нужны: `am.stop()`, `gm.stop()`.
2. **Refresh Rate:** Можно задать частоту (20ms - 1000ms). Для игр используй 20-50ms, для параллакса — 100ms+.
