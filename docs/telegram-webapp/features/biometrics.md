# Biometrics Manager

> **Позволяет использовать FaceID / TouchID для подтверждения действий.**

Идеально для:
- Доступа к кошельку/личным данным
- Подтверждения оплаты
- Входа в "секретную" часть приложения

---

## 🛠️ Init & Check

Сначала нужно инициализировать менеджер и проверить доступность.

```typescript
const bm = WebApp.BiometricManager

bm.init(() => {
  if (bm.isBiometricAvailable) {
    console.log(`Type: ${bm.biometricType}`) // 'face' | 'finger'
    console.log(`Access: ${bm.isAccessGranted}`) // true | false
  }
})
```

## 🔐 Запрос доступа

Если `isAccessGranted: false`, нужно запросить права.

```typescript
bm.requestAccess({
  reason: 'Разреши вход по лицу для защиты данных'
}, (granted) => {
  if (granted) console.log('Ура, доступ есть!')
})
```

## 🔑 Аутентификация

Сам процесс проверки (показ системного UI).

```typescript
bm.authenticate({
  reason: 'Подтвердите операцию'
}, (success, token) => {
  if (success) {
    // token - уникальный токен сессии (если нужен для бэка)
    doSecureAction()
  } else {
    showError('Не удалось подтвердить')
  }
})
```

## ⚙️ Settings

Можно открывать настройки биометрии Telegram.

```typescript
bm.openSettings()
```
